import { StripePaymentRepository, StripePaymentEntity } from "../../infrastructure/persistence/repositories/sro_dev/StripePaymentRepository";


export default async function CreatePaymentIntent(id: string, stripePaymentRepository: StripePaymentRepository) {

    //await stripePaymentRepository.add(stripePaymentEntity);
}